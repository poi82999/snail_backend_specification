# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0

# --- DB / ORM ---
sqlalchemy[asyncio]==2.0.36
alembic==1.13.3
asyncpg==0.30.0
pgvector==0.3.6

# --- Validation / settings ---
pydantic==2.9.2
pydantic-settings==2.6.0
email-validator==2.2.0

# --- Auth ---
pyjwt[crypto]==2.9.0
cryptography==43.0.3
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                 # passlib 1.7.x compatibility

# --- LLM / HTTP ---
openai==1.54.0
httpx==0.27.2
h2==4.1.0
tenacity==9.0.0

# --- Queue / cache ---
redis[hiredis]==5.1.1
arq==0.26.1                # Redis-based job queue (BackgroundTasks 대체)

# --- GCP ---
google-cloud-storage==2.18.2
google-cloud-secret-manager==2.21.0

# --- Notifications ---
firebase-admin==6.5.0

# --- Image ---
Pillow==11.0.0

# --- Observability ---
sentry-sdk[fastapi]==2.18.0
structlog==24.4.0

# --- Misc ---
tzdata==2024.2             # Windows zoneinfo 지원
python-multipart==0.0.12   # 파일 업로드
